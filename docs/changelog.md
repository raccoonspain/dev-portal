# 🗓 История изменений

> Хронология того, **что уже сделано** — новое сверху.
> Каждая запись = один осмысленный шаг. Файл **только дописывается**.

<!-- Новые записи добавляй СВЕРХУ, сразу под этой строкой -->

## 2026-06-27 — Панель подробностей проекта (D-006)

- Кнопка ❓ в списке проектов открывает боковую панель деталей
- Вкладки CLAUDE.md / Состояние / История / Решения с рендером markdown
- Сервер: `/api/projects/:name/journal` теперь отдаёт `CLAUDE.md` из корня проекта
- Кнопка «Выйти» зафиксирована внизу сайдбара (position: fixed)
- Вкладка CLAUDE.md добавлена на страницу dev-portal
- Чат Claude в сайдбаре сворачивается/разворачивается стрелкой

## 2026-06-27 — Вкладка «🅰 dev-portal» в сайдбаре (D-005)

- Новая навигационная вкладка выше «Проекты»: путь `/home/deploy/dev-portal` с копированием, файловый браузер, кнопка VS Code
- API `/api/portal/{open,tree,file,journal}` — выделенное пространство маршрутов для портала
- Файловое дерево исключает `node_modules`, `.git`, `data`
- Встроенный журнал портала: три вкладки Состояние / История / Решения прямо из `docs/`
- Переиспользован существующий file browser через тип `'portal'` в `fbContext`

## 2026-06-27 — Журнал dev-portal: инициализация

- Создан git-репо, `.gitignore` (node_modules, data, .env)
- Создан `scripts/snapshot.sh` для быстрых снимков
- Инициализированы `docs/`: project-brief, state, changelog, decisions, handoff
- Обновлён `CLAUDE.md` — добавлен раздел журналирования и коммитов
- Добавлены `.claude/commands/`: `/log`, `/decision`, `/handoff`

## 2026-06-27 — Страница «Журнал» в портале

- Новая страница `#page-journal` в сайдбаре
- Выбор проекта → четыре вкладки: Состояние / История / Решения / Передача
- Встроенный markdown-рендер (заголовки, списки, цитаты, код, чекбоксы)
- Добавление записи в changelog через UI — вставляет с датой выше маркера
- Бэкенд: `GET /api/projects/:name/journal`, `POST /api/projects/:name/journal/entry`

## 2026-06-27 — Кнопка VS Code для шаблонов

- Добавлен `GET /api/templates/:name/open` — возвращает внешний URL code-server
- ⌨️ кнопка появилась в таблице шаблонов и в файловом браузере для шаблонов
- Рефактор: единая функция `openInVSCode(type, name)` вместо двух

## 2026-06-27 — Исправление URL VS Code

- `CODE_SERVER_URL=http://localhost:8080` — внутренний адрес, браузер не доступен
- Теперь URL строится из заголовков запроса (`x-forwarded-proto` + `x-forwarded-host`)
- Итоговый URL: `https://vibe.blackboxbegin.space/vscode/?folder=/projects/...`

## 2026-06-27 — Таблицы проектов и шаблонов, файловый браузер

- Карточки заменены на таблицы: Название / Описание / Полный путь / Действия
- Описание берётся из `annotation.md` в папке проекта/шаблона
- Полный путь — кликабельный, копирует в буфер (для SSH + VS Code локально)
- Кнопки проектов: 📂 обзор · ⌨️ VS Code · 📥 ZIP · 🗑️ удалить
- Кнопки шаблонов: 📂 обзор · ⌨️ VS Code · + создать проект
- Файловый браузер: панель поверх контента, дерево файлов слева, чтение справа
- Бэкенд: `/tree`, `/file`, `/download` для проектов и шаблонов
- Пакет `archiver` для zip-архивов

## 2026-06-27 — Новые шаблоны Битрикс24

- Удалены старые шаблоны (bitrix24-local-app, bitrix24-open-lines, bitrix24-rest-widget)
- Распакованы `_b24-single-php` и `_base_universal` из zip-архивов
- Созданы `annotation.md` для всех шаблонов
