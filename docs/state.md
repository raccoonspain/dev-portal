# 📍 Состояние проекта — где мы сейчас

> **Живой снимок.** Перезаписывается после каждого шага — здесь только «сейчас».
> Вся история — в [changelog.md](./changelog.md).

**Последнее обновление:** 2026-06-27
**Фаза:** Активная разработка

---

## Где мы сейчас

Базовый портал полностью работает. Реализованы: таблицы проектов и шаблонов с обзором файлов, открытие в VS Code, скачивание ZIP, страница журнала с markdown-рендером и добавлением записей. Сервер запущен через nohup (systemd не перезапускается без sudo). Журнал самого dev-portal только инициализирован.

## Сейчас в работе

Настройка журналирования dev-portal: CLAUDE.md обновлён, docs/ инициализирован, git-репо создано.

## Следующие шаги

- [ ] Перевести сервис обратно на systemd (`sudo systemctl reset-failed dev-portal && sudo systemctl start dev-portal`)
- [ ] Настроить автозапуск code-server при перезагрузке (аналогично)
- [ ] Разобрать дальнейшие фичи портала по приоритету

## Открытые вопросы / блокеры

- systemd для dev-portal и code-server не управляется без sudo — сейчас оба работают через nohup

## Карта проекта

| Что | Где |
|-----|-----|
| Бэкенд (всё API) | `src/server.js` |
| Фронтенд (SPA) | `public/js/app.js` |
| Стили | `public/css/style.css` |
| HTML-оболочка | `public/index.html` |
| БД чатов | `data/portal.db` |
| Переменные окружения | `.env` |
| Шаблоны проектов | `/templates/` |
| Проекты | `/projects/` |
| Прод | `https://vibe.blackboxbegin.space` |
